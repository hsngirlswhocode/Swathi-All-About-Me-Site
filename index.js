const guess = 6;
const wLen = 5;
let numGuess = 0;
let secret = [TULIP, SEVEN, ELITE, LAMBS];
let answer = "";

let currentW = [];
let currentG = 0;

function game () {
  answer = secret[(int) (Math.random()*secret.length)];
  for (int i = 0; i < guess; i++) {
    
  }
}
